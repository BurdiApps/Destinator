/*
 Destinator iOS — Color Theme
 Matches the pink/lavender + purple accent from the wireframes.
*/

import SwiftUI

struct AppTheme {
    // Background bars (top/bottom pink bands)
    static let backgroundPink = Color(red: 0.95, green: 0.85, blue: 0.92)

    // Primary accent (purple action buttons)
    static let accentPurple = Color(red: 0.55, green: 0.36, blue: 0.55)

    // Text colors
    static let textPrimary = Color.black
    static let textSecondary = Color.gray

    // Map placeholder / card background
    static let cardBackground = Color(red: 0.95, green: 0.95, blue: 0.95)

    // Nav icon color
    static let navIcon = Color(red: 0.4, green: 0.4, blue: 0.45)

    // Screen background
    static let screenBackground = Color.white
}
